#!/usr/bin/env bash

ROOT="$(cd "$(dirname "$0")" && pwd)"

case "$1" in
  ask)
    shift
    PYTHONPATH="$ROOT" python3 -m ima.runtime.runtime "$@"
    ;;
  stream)
    tail -n 50 "$ROOT/.ima/ima.stream.jsonl"
    ;;
  *)
    echo "IMA:"
    echo "  ask \"query\""
    echo "  stream"
    ;;
esac
