from founder.executive_ai.community.community_operating_system import (
    process_community_action
)


def route_event(
    platform,
    user,
    message
):

    return process_community_action(
        user,
        user,
        platform,
        message
    )
